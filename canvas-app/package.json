{
  "name": "discov-canvas-app",
  "version": "1.0.0",
  "private": true,
  "description": "Pre-built Excalidraw bundle for Discov Canvas",
  "scripts": {
    "build": "node build.mjs",
    "package": "cd dist && tar -czf ../excalidraw-bundle.tgz ."
  },
  "dependencies": {
    "@excalidraw/excalidraw": "^0.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
